@page "/workspace/import-profile"
@inject NavigationManager Navigation

<PageTitle>Redirecting</PageTitle>

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            Navigation.NavigateTo("/setup/linkedin", replace: true);
        }
    }
}

